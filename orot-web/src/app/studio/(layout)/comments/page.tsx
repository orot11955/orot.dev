import { CommentsManagementPage } from '@/components/studio/comments/CommentsManagementPage';

export const metadata = {
  title: '댓글 관리 · orot.studio',
};

export default function StudioCommentsPage() {
  return <CommentsManagementPage />;
}
