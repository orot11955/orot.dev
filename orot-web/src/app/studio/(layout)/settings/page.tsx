import { SettingsPage } from '@/components/studio/settings/SettingsPage';

export const metadata = {
  title: '설정 · orot.studio',
};

export default function StudioSettingsPage() {
  return <SettingsPage />;
}
